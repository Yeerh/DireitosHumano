export type AppUser = {
  id: string;
  cpf: string;
  email: string;
  numero: string;
  nome: string;
  login: string;
  senha: string;
  createdAt: string;
};

export type SessionData = {
  userId: string;
};
